import { ComponentFixture, TestBed } from '@angular/core/testing';
import { of } from 'rxjs';
import { HttpHeaders, HttpResponse } from '@angular/common/http';
import { ActivatedRoute, convertToParamMap } from '@angular/router';

import { ProjecunitmangmentTestModule } from '../../../test.module';
import { SupervisorComponent } from 'app/entities/supervisor/supervisor.component';
import { SupervisorService } from 'app/entities/supervisor/supervisor.service';
import { Supervisor } from 'app/shared/model/supervisor.model';

describe('Component Tests', () => {
  describe('Supervisor Management Component', () => {
    let comp: SupervisorComponent;
    let fixture: ComponentFixture<SupervisorComponent>;
    let service: SupervisorService;

    beforeEach(() => {
      TestBed.configureTestingModule({
        imports: [ProjecunitmangmentTestModule],
        declarations: [SupervisorComponent],
        providers: [
          {
            provide: ActivatedRoute,
            useValue: {
              data: of({
                defaultSort: 'id,asc',
              }),
              queryParamMap: of(
                convertToParamMap({
                  page: '1',
                  size: '1',
                  sort: 'id,desc',
                })
              ),
            },
          },
        ],
      })
        .overrideTemplate(SupervisorComponent, '')
        .compileComponents();

      fixture = TestBed.createComponent(SupervisorComponent);
      comp = fixture.componentInstance;
      service = fixture.debugElement.injector.get(SupervisorService);
    });

    it('Should call load all on init', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new Supervisor(123)],
            headers,
          })
        )
      );

      // WHEN
      comp.ngOnInit();

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.supervisors && comp.supervisors[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should load a page', () => {
      // GIVEN
      const headers = new HttpHeaders().append('link', 'link;link');
      spyOn(service, 'query').and.returnValue(
        of(
          new HttpResponse({
            body: [new Supervisor(123)],
            headers,
          })
        )
      );

      // WHEN
      comp.loadPage(1);

      // THEN
      expect(service.query).toHaveBeenCalled();
      expect(comp.supervisors && comp.supervisors[0]).toEqual(jasmine.objectContaining({ id: 123 }));
    });

    it('should calculate the sort attribute for an id', () => {
      // WHEN
      comp.ngOnInit();
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['id,desc']);
    });

    it('should calculate the sort attribute for a non-id attribute', () => {
      // INIT
      comp.ngOnInit();

      // GIVEN
      comp.predicate = 'name';

      // WHEN
      const result = comp.sort();

      // THEN
      expect(result).toEqual(['name,desc', 'id']);
    });
  });
});
